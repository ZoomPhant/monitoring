This folder contains wrapping docker files that try to publish pre-build released docker images from internal registry to public registry like docker hub.

**Note**: we use wrapping docker file so we can pre-set necessary environment or adding other assets for ease of use.